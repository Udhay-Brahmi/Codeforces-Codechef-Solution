#include <bits/stdc++.h>
using namespace std;
int main(){;
    int a,b;
    cin>>a>>b;char c;
    for(int i=0;i<a*b;i++){
        cin>>c;
        if(c=='C' || c=='M' || c=='Y'){cout<<"#Color"<<endl;return 0;}
    }
    cout<<"#Black&White"<<endl;
    return 0;
}
