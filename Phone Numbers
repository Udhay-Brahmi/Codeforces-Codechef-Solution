#include <bits/stdc++.h>
using namespace std;
int main(){
    int n;
    string h;
    cin>>n>>h;
    int y = n/11,c=0;
    for(int i=0;i<h.length();i++){
       if(h[i]=='8'){c++;}
    }
    cout<<min(y,c)<<endl;
    return 0;
}
