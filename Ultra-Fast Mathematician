#include <bits/stdc++.h>
using namespace std;
int main(){
  string h,j,k="";
  cin>>h>>j;
  for(int i=0;i<h.length();i++){
      if(h[i]!=j[i]){k+='1';}
      else{k+='0';}
  }
  cout<<k<<endl;
  return 0;
}
