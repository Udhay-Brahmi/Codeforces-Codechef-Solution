#include <bits/stdc++.h>
using namespace std;
int main(){
    string h;
    cin>>h;
    map<char , int> m;
    for(int i=0;i<h.length();i++){
       m[h[i]]++;
    }
    if(m.size()%2!=0){cout<<"IGNORE HIM!"<<endl;}
    else{cout<<"CHAT WITH HER!"<<endl;}
    return 0;
}
