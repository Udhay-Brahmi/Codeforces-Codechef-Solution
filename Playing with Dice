#include <bits/stdc++.h>
using namespace std;
int main(){
    int a,b;
    cin>>a>>b;
    int w=0,d=0,s=0;
    for(int i=1;i<=6;i++){
        if(abs(i-a)<abs(i-b)){w++;}
        else if(abs(i-a)==abs(i-b)){d++;}
        else{s++;}
    }
    cout<<w<<" "<<d<<" "<<s<<endl;
    return 0;
}
