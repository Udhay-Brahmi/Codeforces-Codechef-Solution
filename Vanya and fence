#include <bits/stdc++.h>
using namespace std;
int main(){
    int n,m,d;cin>>n>>m;
    int arr[n],sum=0;
    for(int i=0;i<n;i++){
        cin>>d;
        if(d>m){sum+=2;}
        else{sum+=1;}
    }
    cout<<sum<<endl;
}
