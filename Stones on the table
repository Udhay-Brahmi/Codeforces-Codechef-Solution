#include <bits/stdc++.h>
using namespace std;
int main(){
    int n,k=0;cin>>n;
    string h;cin>>h;
    for(int i=0;i<(n-1);i++){
        if(h[i]==h[i+1]){k++;}
    }
    cout<<k<<endl;
    return 0;
}
